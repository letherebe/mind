```javascript
// Simulates cloud-to-music mapping: click to generate a melody based on random "cloud" weather data
const notes = ['C4','D4','E4','F4','G4','A4','B4','C5'];
function playTone(freq, time) {
  const ctx=new(window.AudioContext||window.webkitAudioContext)(),o=ctx.createOscillator(),g=ctx.createGain();
  o.type='sine'; o.frequency.value=freq; o.connect(g); g.connect(ctx.destination);
  g.gain.setValueAtTime(0.2,ctx.currentTime);g.gain.linearRampToValueAtTime(0,ctx.currentTime+time);
  o.start(); o.stop(ctx.currentTime+time);
}
function noteToFreq(n){return 261.63*Math.pow(2,notes.indexOf(n)/12);}
document.body.onclick=()=>{
  let contour=Math.floor(Math.random()*5)+3, wind=Math.random()*1.5+0.5, bright=Math.random();
  let key=bright>0.5?notes:['C4','D4','Eb4','F4','G4','Ab4','Bb4','C5'];
  for(let i=0;i<contour;i++){
    setTimeout(()=>playTone(noteToFreq(key[Math.floor(Math.random()*key.length)]

<!-- truncated by token limit -->