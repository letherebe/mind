```javascript
// Animated, drifting clouds—generative, ever-changing shapes on canvas
const canvas = document.createElement('canvas');
document.body.style.margin = 0;
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;
document.body.appendChild(canvas);
const ctx = canvas.getContext('2d');

let clouds = Array.from({length: 7}, () => ({
  x: Math.random() * canvas.width,
  y: Math.random() * canvas.height / 2,
  r: 80 + Math.random() * 120,
  dx: 0.3 + Math.random() * 0.4,
  op: 0.17 + Math.random() * 0.12
}));

function draw() {
  ctx.clearRect(0,0,canvas.width,canvas.height);
  for(let c of clouds){
    ctx.beginPath();
    for(let i=0;i<6;i++){
      let angle = i/6*2*Math.PI;
      let px = c.x + Math.cos(angle) *

<!-- truncated by token limit -->